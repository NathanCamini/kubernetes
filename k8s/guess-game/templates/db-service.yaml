apiVersion: v1
kind: Service
metadata:
  name: db
  labels:
    app: guess-game
    component: db
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.db.service.port }}
      targetPort: {{ .Values.db.service.port }}
      protocol: TCP
  selector:
    app: guess-game
    component: db
