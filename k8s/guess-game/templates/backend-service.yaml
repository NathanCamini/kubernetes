apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    app: guess-game
    component: backend
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: {{ .Values.backend.service.port }}
      protocol: TCP
  selector:
    app: guess-game
    component: backend
